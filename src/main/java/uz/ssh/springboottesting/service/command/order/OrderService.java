package uz.ssh.springboottesting.service.command.order;

import org.springframework.stereotype.Service;

@Service
public class OrderService {
//    private final OrderRepository repo;
//    private final EventStore eventStore;

//    public OrderService(OrderRepository repo, EventStore eventStore) {
//        this.repo = repo;
//        this.eventStore = eventStore;
//    }

    public void createOrder(OrderPayload payload) {
//        Order order = new Order(payload.getCustomerId(), payload.getItems());
//        repo.save(order);
//        eventStore.save("CreateOrderCommand", payload); // record intent

    }
}